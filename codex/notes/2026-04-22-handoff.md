# Elowen Handoff - 2026-04-23

## Current State

- Slice set `0` through `39` is complete on the current Slice 39 branch.
- Slice set `0` through `38` is complete on local `main`.
- `Slice 37 - Notes Retrieval And Context Expansion` is closed out on local `main` in `elowen-api`, `elowen-notes`, and `elowen-workspace`.
- `Slice 38 - Tools And Integration Expansion` is closed out on local `main` in `elowen-api`, `elowen-edge`, `elowen-platform`, `elowen-ui`, and `elowen-workspace`.
- `Slice 39 - Kubernetes Deployment Validation` is closed out on this branch in `elowen-platform` and `elowen-workspace`.

## Slice 37 Closeout Summary

- `elowen-notes` merged Slice 37 work adds weighted retrieval inputs, preferred-note and preferred-source boosts, and retrieval metadata so note search can rank promoted memory more intelligently than recency-only ordering.
- `elowen-api` merged Slice 37 work replaces the flat related-note conversational input with a richer memory bundle that prioritizes direct thread/job promotions, merges retrieval reasons, and expands top note bodies into assistant context.
- `elowen-workspace` merged Slice 37 work updates the roadmap, README current-state summary, and workspace submodule pointers to the merged child repo revisions.

## Validation Summary

- Automated validation passed for Slice 37:
  - `cargo check` in `elowen-notes`
  - `cargo fmt --check` in `elowen-notes`
  - `cargo test --quiet` in `elowen-notes`
  - `cargo clippy --all-targets -- -D warnings` in `elowen-notes`
  - `cargo doc --no-deps` in `elowen-notes`
  - `cargo check` in `elowen-api`
  - `cargo fmt --check` in `elowen-api`
  - `cargo test --quiet` in `elowen-api`
  - `cargo clippy --all-targets -- -D warnings` in `elowen-api`
  - `cargo doc --no-deps` in `elowen-api`
- Manual UAT passed on 2026-04-22 against the live authenticated deployment at `https://api.ericburden.dev/`:
  - the VPS deploy completed with `elowen-api:dev-slice37-20260422104206` and `elowen-notes:dev-slice37-20260422104206`
  - operator sign-in succeeded in the live UI
  - summary promotion into notes succeeded from the job detail surface
  - job and thread note surfaces refreshed to show the promoted note
  - conversational follow-up in the same thread grounded on the promoted memory rather than treating it as archival-only context
- Slice 38 validation and closeout also passed:
  - automated validation passed in the touched Rust repos during Slice 38 implementation
  - Docker Compose builds for `elowen-api` and `elowen-edge` passed after updating build contexts and Dockerfiles for the shared contracts dependency
  - manual local UAT passed on 2026-04-23 for both capability and repository job paths
  - capability UAT thread/job: `01KPVB6EMRCH0T2KXAXBYTC33X` / `01KPVB7FRKZ9FMMHZ64NST3SA7`
  - repository UAT thread/job: `01KPXCG5SW4GH2A4H45QBJ33WH` / `01KPXCGBJGHMXPSH1M64MK78QZ`
  - the final repository UAT completed with successful worktree creation plus passing `cargo check` and `cargo test --quiet` validation inside the disposable worktree
  - GitHub follow-up issues `#1` and `#2` were closed after Docker build verification and the final repository-job UAT pass
- Slice 39 validation and closeout also passed:
  - `kubectl kustomize elowen-platform/k8s/base` rendered successfully after the audit updates
  - the validated base applied successfully to a local `kind` cluster on 2026-04-23
  - `postgres`, `nats`, and `arangodb` started successfully with the current manifests
  - `elowen-api`, `elowen-notes`, and `elowen-ui` reached `Running` after their images were loaded into the cluster
  - direct HTTP probes through `kubectl port-forward` returned `200 OK` for API `/health`, notes `/health`, and the UI root
  - the first concrete deployment blocker was private GHCR image access, which is now documented as an operator prerequisite
  - GitHub milestone issues `#3`, `#5`, `#6`, and `#7` were closed after the audit, topology decision, deployment-input pass, and cluster validation

## Latest Known-Good Revisions

- `elowen-api` merged `main`: `ae4d951`
- `elowen-ui` merged `main`: `e19398e`
- `elowen-edge` merged `main`: `94a4c2c`
- `elowen-notes` merged `main`: `1e0e0ad`
- `elowen-platform` merged `main`: `4963e1a`

## Next Resume Focus

1. Merge Slice 39 and update workspace pointers on `main`.
2. Start `Slice 40 - CI Workflow Maintenance`.
3. Keep the Slice 38 prompt-first generic-job model and the Slice 39 supported Kubernetes topology as the baseline for future deployment and integration work.

## Slice 38 Closeout Notes

- `elowen-platform/contracts/rust/elowen-contracts` now models job targets explicitly with `repository` and `capability` specializations.
- The transport shape has been simplified toward a prompt-first execution envelope: `target_kind`, `target_name`, optional repo-only branch context, and a rich textual `prompt`.
- `elowen-api` now persists `target_kind` plus optional `capability_name`, routes capability jobs by device capabilities, and can emit capability-targeted conversational drafts.
- `elowen-edge` now accepts capability jobs, runs them without a git worktree, and suppresses commit/push approval for that path while preserving lifecycle and summary reporting.
- `elowen-ui` now renders capability drafts distinctly from repository drafts and can dispatch a capability-targeted generic job from the thread transcript.
- `elowen-platform/compose/docker-compose.yml` now mounts the parent repo root for the local Linux edge container so git worktree and submodule `.git` paths resolve correctly during repository-job UAT.
- The final local Compose UAT verified both the non-repo capability path and the repository worktree path, and the follow-up GitHub issues created during validation are now closed.
- Provider-specific integrations such as Calendar, Gmail, and Drive remain deferred; the first delivered non-repo vertical is the generic `generic_jobs` capability.
