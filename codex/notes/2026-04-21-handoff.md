# Elowen Handoff - 2026-04-21

## Current State

- Slice set `0` through `35` is complete on merged `main`.
- `Slice 35 - Service-Grade Laptop Edge Runtime` is closed out on merged `main` in `elowen-edge`, `elowen-platform`, and `elowen-workspace`.
- The next planned slice is `Slice 36 - Shared Rust Message Contracts`.

## Slice 35 Closeout Summary

- `elowen-edge` merged Slice 35 work includes:
  - a supervised Windows wrapper loop in `Start-ElowenEdge.ps1` that keeps the SSH tunnel and edge runtime aligned and restarts the pair after recoverable failure
  - a scheduled-task installer in `Register-ElowenEdgeTask.ps1` that prefers `Startup + S4U`, falls back to `LogOn + Interactive` on access-denied hosts unless `-RequireServiceGrade` is used, and prints the effective installed mode
  - a broader documentation pass across runtime, config, discovery, sandbox, registration, execution, README guidance, and Windows runtime scripts
  - a small trust-registration refactor that keeps `clippy -D warnings` green while preserving the registration payload shape
- `elowen-platform` merged Slice 35 work includes:
  - a rewritten `docs/laptop-edge.md` runbook that makes Scheduled Task the preferred Windows host model
  - explicit documentation that Startup-folder launch is fallback-only guidance
  - operator notes for the new fallback behavior when `Startup + S4U` registration is denied by the current host or session

## Validation Summary

- Automated validation passed in `elowen-edge`:
  - `cargo fmt --check`
  - `cargo clippy --all-targets -- -D warnings`
  - `cargo test --quiet`
  - `cargo doc --no-deps`
- Manual UAT passed on 2026-04-21 against the current Windows host:
  - environment: local `slice/35-service-grade-laptop-edge-runtime` worktree, release `elowen-edge` build, temporary scheduled task, real SSH tunnel target `api.ericburden.dev`, and live trusted registration as `elowen-desktop`
  - preferred `Startup + S4U` scheduled-task install from the current non-elevated session: fail with expected host-policy boundary
    notes: the installer now handles this by falling back to `LogOn + Interactive` unless `-RequireServiceGrade` is supplied
  - fallback scheduled task launches the Slice 35 wrapper, `ssh.exe`, and the Slice 35 `elowen-edge.exe` build: pass
    notes: the task reached `Running` and the child processes were visible under Task Scheduler supervision
  - trusted registration succeeds through the scheduled-task path: pass
    notes: `elowen-edge-stdout.log` recorded successful registration and registration heartbeat for `elowen-desktop`
  - forced runtime failure recovers under wrapper supervision: pass
    notes: after terminating the first `elowen-edge.exe` process, the wrapper relaunched the runtime and the replacement instance re-registered successfully
  - real job dispatch from the live orchestrator: not run
    notes: no live operator auth credentials were available locally to create a real remote job session during the UAT window

## Latest Known-Good Revisions

- `elowen-api` merged `main`: `a2e2ba2`
- `elowen-ui` merged `main`: `e19398e`
- `elowen-edge` merged `main`: `7850d2a`
- `elowen-notes` merged `main`: `102630e`
- `elowen-platform` merged `main`: `d80a7b1`

## Next Resume Focus

1. Start `Slice 36 - Shared Rust Message Contracts`.
2. Verify the current local `main` branches remain clean in `elowen-edge`, `elowen-platform`, and `elowen-workspace` before the next slice starts.
3. Keep manual UAT evidence for Slice 35 under `C:\Users\ericw\AppData\Local\Elowen\edge-slice35-uat` until it is no longer needed.
